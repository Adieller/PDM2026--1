package edu.ipn.upiita.pdm.proyecto1.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Oscura Moderna
val DarkBackground = Color(0xFF1A1C20) // Fondo principal
val DarkSurface = Color(0xFF252830)    // Color de las tarjetas
val DarkPrimary = Color(0xFF4DB6AC)    // Acento principal (Turquesa/Verde)
val TextPrimaryDark = Color(0xFFF0F0F0) // Texto principal
val TextSecondaryDark = Color(0xFFB0B0B0) // Texto secundario (fechas)

// Colores de Estatus
val StatusCompleted = Color(0xFF4CAF50)
val StatusPending = Color(0xFFFFA000)
val StatusInProgress = Color(0xFF29B6F6)
val StatusOverdue = Color(0xFFE57373)
val StatusOnTime = Color(0xFF4DB6AC) // Mismo que el primario
